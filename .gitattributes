# Keep line endings sane across Windows and Linux.
* text=auto eol=lf

# Windows scripts must keep CRLF or they break when run.
*.bat text eol=crlf
*.vbs text eol=crlf

# Never touch binaries.
*.png binary
*.ico binary
*.pdf binary
*.zip binary
